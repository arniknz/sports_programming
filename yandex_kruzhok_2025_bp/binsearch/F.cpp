#include <bits/stdc++.h>

using namespace std;

using ll = long long;

int main() {
    ll n, m, k;
    vector<ll> a(n), p;
    for (auto &x : a) {cin >> x;}
    while(m--) {
        cin >> k;
    }
    return 0;
}